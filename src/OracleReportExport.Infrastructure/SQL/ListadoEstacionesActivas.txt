  SELECT CODESTACION, DESESTACION, CONNECTION_STRING
    FROM INSTANCIAS_BBDD
   WHERE  ACTIVO = -1   AND  
    (DESESTACION LIKE '%I.T.V.%' OR  DESESTACION LIKE '%U.M.A.%' )
    AND (CONNECTION_STRING IS NOT NULL OR LENGTH(CONNECTION_STRING)>0)
ORDER BY 1